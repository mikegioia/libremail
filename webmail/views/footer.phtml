    <div class="top-layer u-cf">
        <div id="topnav-actions" class="dropdown u-pull-right">
            <div class="status u-pull-left">
                <div class="running">
                <?php
                if ($meta->running == 1): ?>
                    <i class="fa fa-wifi"></i>
                    <span>Online</span>
                <?php
                else: ?>
                    <i class="fa fa-power-off"></i>
                    <span><strong>Offline!</strong></span>
                <?php
                endif; ?>
                </div>
                <div class="asleep">
                <?php
                if ($meta->asleep == 1 || $meta->running != 1): ?>
                    <i class="fa fa-moon"></i>
                    <span>Asleep</span>
                <?php
                else: ?>
                    <i class="fa fa-sync-alt"></i>
                    <span><strong>Syncing</strong></span>
                <?php
                endif; ?>
                </div>
            </div>
            <a class="icon button" href="#" tabindex="3">
                <i class="fa fa-sliders-h"></i>
            </a>
            <ul class="right-menu size-medium">
                <li>
                    <i class="fa left-icon fa-wrench"></i>
                    <a href="#settings">Settings &amp; Preferences</a>
                </li>
                <li>
                    <i class="fa left-icon fa-envelope"></i>
                    <a href="#account">Configure Email</a>
                </li>
            </ul>
        </div>
    </div>
<?php
if (isset($alertMessage) && $alertMessage): ?>
    <div class="notifications">
        <input type="checkbox" id="notif-1" />
        <div class="notif" for="notif-1">
            <span class="message"><?php echo $alertMessage; ?></span>
            <label for="notif-1">
                <i class="fa fa-times"></i>
            </label>
            <!--<a href="#">Undo</a>-->
        </div>
    </div>
<?php
endif; ?>
</body>
</html>